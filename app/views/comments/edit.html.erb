<h1>Edit reply</h1>
<div>
	<%= form_for([@post, @comment]) do |f| %>
	  <div class="field">
	    <%= f.label :comment %><br />
	    <%= f.text_field :comment %>
	  </div>
	  <div class="actions">
	    <%= f.submit 'Edit comment' %>
	  </div>
	<% end %>
</div>